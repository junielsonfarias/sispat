# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Projeto usa pnpm — bloquear lockfiles de npm/yarn para não conflitar
package-lock.json
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Evaluation screenshots
screenshot.png
.eslintcache

# Environment variables (segredos), mas versiona os templates .env.example
.env*
!.env.example
!**/.env.example
!.env.docker.example

# Claude Code
.claude/

# Cobertura de testes (jest/vitest --coverage)
coverage/

# Backups de banco de dados (podem conter dados sensíveis — nunca versionar)
backend/backups/
/backup/
*.sql.zip
*.dump

# Documentos de referência local (Projeto de Lei, Decreto, etc.) — não versionar
Docs/analise/
image.png
